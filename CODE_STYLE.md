# 코드 작성 규칙

프로젝트에서 작성하는 C++ 코드와 GLSL 셰이더는 줄바꿈을 충분히 사용한다.
자동 정렬 기준은 루트의 `.clang-format`이며 clang-format 22를 기준으로 설정했다.

- 함수·클래스·구조체·제어문의 여는 중괄호는 다음 줄에 둔다(Allman).
- 한 줄에는 하나의 문장만 작성한다. 여러 변수의 선언도 각 줄로 분리한다.
- `if`, `else`, `for`, `while`의 본문이 한 문장이더라도 중괄호를 사용한다.
- 짧은 함수, 접근자, 람다도 본문을 한 줄로 압축하지 않는다.
- 함수와 타입 정의 사이에는 빈 줄을 둔다. 함수 안에서는 처리 단계가 바뀔 때
  빈 줄을 사용하며, 연속된 빈 줄은 하나까지만 둔다.
- 들여쓰기는 공백 4칸, 한 줄 길이는 100자를 기준으로 한다.
- 길어서 줄을 나눈 함수 인수와 매개변수는 한 줄에 하나씩 배치한다.
- 생성자의 초기화 목록은 항목별로 줄을 나눈다.
- include 순서는 유지한다. `.cpp`의 `stdafx.h`를 첫 번째 include로 유지한다.
- 한글 문자열을 위해 UTF-8을 사용한다. 소스의 줄 끝은 CRLF로 통일한다.

```cpp
void Update(float deltaTime)
{
    if (!m_Ready)
    {
        return;
    }

    UpdateWorld(deltaTime);
    UpdatePlayer(deltaTime);
}
```

적용 범위는 `SimpleGame` 바로 아래의 자체 `.cpp`·`.h`와 `SimpleGame/Shaders`의
`.vs`·`.fs`다. `Dependencies`의 외부 라이브러리와 빌드 출력 폴더는 정렬 대상에서 제외한다.
clang-format 실행 시 이 범위의 파일만 명시적으로 전달한다.
포맷터가 처리하지 않는 변수 선언 분리와 처리 단계 사이의 빈 줄은 작성 시 지킨다.

컨벤션 변경은 이름·조건·수치·실행 순서 등 게임 동작을 바꾸지 않는 범위에서 진행한다.
별도 요청이 없다면 빌드와 실행 결과 확인은 사용자가 담당한다.
